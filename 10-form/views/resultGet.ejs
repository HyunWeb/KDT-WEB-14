<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Form Result Get</title>
  </head>
  <body>
    <h1><%= title %> 폼 결과 확인하기</h1>

    <div><span><%= userInfo.name %></span>님 환영합니다!</div>
    <div>성별은 <span><%= userInfo.gender %></span></div>
    <% if (userInfo.year && userInfo.month && userInfo.day) { %>
    <div>
      생년월일은 <span><%= userInfo.year %></span>년
      <span><%= userInfo.month %> </span>월 <span><%= userInfo.day %> </span>일
    </div>
    <% } %> <% if (userInfo.hobby) { %>
    <div>
      <% for(let i = 0; i < userInfo.hobby.length; i++){ %>
      <span><%= userInfo.hobby[i] %> </span>
      <% } %> 에 관심이 있으시군요!
    </div>
    <% } %>
  </body>
</html>
