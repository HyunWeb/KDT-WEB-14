<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>loginForm</title>
    <!-- axios CDN -->
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>

    <style>
      body {
        background-color: rgb(32, 32, 32);
      }
      section {
        background-color: rgb(254, 255, 251);
        border-radius: 15px;
        width: 30%;
        margin: 10% auto;
        text-align: center;
        padding: 30px 0;
      }
      h1 {
        font-size: 30px;
      }
      input {
        margin-bottom: 10px;
        width: 40%;
        height: 30px;
      }
      button {
        width: 80px;
        height: 40px;
      }
    </style>
  </head>
  <body>
    <section>
      <div>
        <h1>파일 업로드</h1>
        <form action="/practice" method="post" enctype="multipart/form-data">
          <input type="text" name="userId" id="id" placeholder="아이디" /><br />
          <input
            type="password"
            name="userPw"
            id="pw"
            placeholder="비밀번호"
          /><br />
          <input
            type="text"
            name="userName"
            id="name"
            placeholder="이름"
          /><br />
          <input
            type="number"
            name="userAge"
            id="age"
            placeholder="나이"
          /><br />
          <input type="file" name="userImg" id="img" /><br />
          <button type="button" onclick="uploadD()">회원가입</button>
        </form>
      </div>
      <div>
        <img src="" alt="프로필 이미지" />
        <h1>님! 환영합니다!</h1>
        <p>
          비밀번호 : <br />
          이름 : <br />
          나이 :
        </p>
      </div>
    </section>
    <script>
      async function uploadD() {
        const formData = new FormData();
        const imgfile = document.querySelector("#img");
        formData.append("d_file", imgfile.files[0]);

        let res = await axios({
          method: "post",
          url: "/uploadD",
          data: formData,
          headers: {
            "Content-Type": "multipart.form-data",
          },
        });
        console.log(res);
        const img = document.querySelector("img");
        // img.src = "/" + res.path;
      }
    </script>
  </body>
</html>
